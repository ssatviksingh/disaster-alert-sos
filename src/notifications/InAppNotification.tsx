import { View, StyleSheet } from "react-native";
import AppText from "../common/AppText";

const InAppNotification = () => {
  // placeholder UI (will be wired to store later)
  return null;
};

export default InAppNotification;
